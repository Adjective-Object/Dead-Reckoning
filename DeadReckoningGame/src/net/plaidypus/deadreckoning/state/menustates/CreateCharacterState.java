package net.plaidypus.deadreckoning.state.menustates;

import java.util.ArrayList;

import net.plaidypus.deadreckoning.hudelements.HudElement;
import net.plaidypus.deadreckoning.state.HudLayersState;

/**
 * The Class CreateCharacterState.
 * 
 * TODO totally unimplemented meant to allow for the creation of custom classes.
 */
public class CreateCharacterState extends HudLayersState {

	/**
	 * Instantiates a new creates the character state.
	 * 
	 * @param stateID
	 *            the state id
	 * @param elements
	 *            the elements
	 */
	public CreateCharacterState(int stateID, ArrayList<HudElement> elements) {
		super(stateID, elements);
	}

}
