/*
 * 
 */
package net.plaidypus.deadreckoning;


/**
 * The Class DamageCalculator.
 * In the future, all Damage calculations will be piped through this, but for now, it serves no purpose
 */
public class DamageCalculator {
	/*
	 * public static boolean checkHits(LivingEntity attacker, LivingEntity
	 * defender){ double accuracy =
	 * 1.2*attacker.getDEX()+attacker.getLUK()+attacker.getINT(); double
	 * avoidability = defender.getDEX() + defender.getINT() + 2 *
	 * defender.getLUK(); double hitRate=
	 * 100+Math.sqrt(accuracy)-Math.sqrt(avoidability); return
	 * Utilities.randInt(0,100)<=hitRate; }
	 * 
	 * public static int getPhysicalDamage(LivingEntity attacker, LivingEntity
	 * defender, int attackPower){
	 * 
	 * }
	 * 
	 * public static int getMagicalDamage(LivingEntity attacker, LivingEntity
	 * defender, int attackPower){
	 * 
	 * }
	 * 
	 * public static int calculateMaxDamage(double attackSum, double defenseSum,
	 * int attackPower){
	 * 
	 * }
	 * 
	 * public static int calculateMinDamage(double attackSum, double defenseSum,
	 * int attackPower){ return attackSum-defenseSum }
	 */

}
